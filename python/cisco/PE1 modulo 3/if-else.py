if the_weather_is_good:
    if nice_restaurant_is_found:
        have_lunch()
    else:
        eat_a_sandwich()
else:
    if tickets_are_available:
        go_to_the_theater()
    else:
        go_shopping()

#Si hay buen clima
# saldremos a caminar. 
# Si encontramos un buen restaurante, 
# almorzaremos allí. 
# De lo contrario, vamos a comer un sandwich. 
# Si hay mal clima, 
# iremos al cine. 
# Si no hay boletos, 
# iremos de compras al centro comercial más cercano.